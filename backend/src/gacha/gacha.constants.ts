// Constants are now managed via SystemConfigService (DB + env var fallbacks).
// This file is kept for backward compatibility but re-exports nothing —
// consumers should inject SystemConfigService instead.
